{
  "name": "obsidian-folder-sort-z-to-a",
  "version": "0.2.1",
  "description": "Sort folders Z-to-A in the native File Explorer.",
  "license": "MIT",
  "author": "Viggo Meesters",
  "type": "module",
  "scripts": {
    "build": "NODE_ENV=production node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs",
    "lint": "node scripts/lint.mjs",
    "typecheck": "tsc --noEmit",
    "test": "npm run build:test && node --test tests/*.test.mjs && npm run build && node scripts/smoke.mjs",
    "build:test": "esbuild src/sort.ts --bundle --platform=node --format=esm --outfile=dist/sort.mjs",
    "sync:test-vault": "npm run build && node scripts/sync-test-vault.mjs",
    "smoke:cdp": "node scripts/smoke-cdp.mjs",
    "version": "node version-bump.mjs && git add manifest.json versions.json",
    "community:check": "node scripts/community-check.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "esbuild": "^0.25.12",
    "obsidian": "^1.8.7",
    "typescript": "^5.9.3"
  }
}
